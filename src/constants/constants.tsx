const NAV_BAR_HEIGHT = 56; // px, must match your nav bar minHeight

const GRID_ROWS = 2;
const GRID_COLS = 2;

const INACTIVITY_LIMIT = 5 * 60 * 1000; // 1 minutes

const THEME_KEY = "theme"; // localStorage key

export { NAV_BAR_HEIGHT, GRID_ROWS, GRID_COLS, INACTIVITY_LIMIT, THEME_KEY };
